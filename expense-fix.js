let expenses=JSON.parse(localStorage.getItem('velora-expenses')||'[]');
if(!document.querySelector('[data-section="Expenses"]')){const settings=document.querySelector('[data-section="Settings"]');const item=document.createElement('button');item.className='nav-item';item.dataset.section='Expenses';item.innerHTML='<span class="icon">↘</span>Expenses';settings?.before(item);item.addEventListener('click',renderExpenses)}
function renderExpenses(){document.querySelector('.nav-item.active')?.classList.remove('active');document.querySelector('[data-section="Expenses"]')?.classList.add('active');document.getElementById('subtitle').textContent='Expenses overview for your Velora store.';const currency=localStorage.getItem('velora-currency')?.startsWith('EGP')?'ج.م':'$';const total=expenses.reduce((s,e)=>s+Number(e.amount),0);const sales=86420;openDynamic('Expenses',{actions:'<button class="gold-button" id="exportExpenses">Export expenses</button>',content:`<div class="expense-summary"><div><strong>${currency}${total.toLocaleString()}</strong><span>Total expenses</span></div><div><strong>${currency}${sales.toLocaleString()}</strong><span>Total sales</span></div><div><strong>${currency}${(sales-total).toLocaleString()}</strong><span>Available balance</span></div></div><div class="expense-layout"><form class="expense-form" id="expenseForm"><h3>Record expense</h3><label>Expense title<input name="title" placeholder="e.g. Packaging supplies" required></label><label>Category<select name="category"><option>Inventory</option><option>Marketing</option><option>Shipping</option><option>Rent</option><option>Salaries</option><option>Other</option></select></label><label>Amount<input name="amount" type="number" min="0" step="0.01" placeholder="0.00" required></label><label>Date<input name="date" type="date" required></label><label>Payment method<select name="payment"><option>Cash</option><option>Bank transfer</option><option>Card</option></select></label><label>Notes<textarea name="notes" placeholder="Optional notes"></textarea></label><button class="account-action" type="submit">Save expense</button></form><section class="expenses-table"><div class="table-head"><span>Expense</span><span>Category</span><span>Date</span><span>Payment</span><span>Amount</span><span></span></div>${expenses.map(e=>`<div class="expense-row"><div><strong>${e.title}</strong><small>${e.notes||'No notes'}</small></div><span>${e.category}</span><span>${e.date}</span><span>${e.payment}</span><strong>${currency}${Number(e.amount).toLocaleString()}</strong><button data-delete-expense="${e.id}">Delete</button></div>`).join('')||'<div class="empty-comments">No expenses recorded yet.</div>'}</section></div>`});document.getElementById('expenseForm').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);expenses=[{id:Date.now(),title:d.get('title'),category:d.get('category'),amount:Number(d.get('amount')),date:d.get('date'),payment:d.get('payment'),notes:d.get('notes')},...expenses];localStorage.setItem('velora-expenses',JSON.stringify(expenses));renderExpenses();showToast('Expense saved successfully')});document.querySelectorAll('[data-delete-expense]').forEach(b=>b.addEventListener('click',()=>{expenses=expenses.filter(e=>e.id!==Number(b.dataset.deleteExpense));localStorage.setItem('velora-expenses',JSON.stringify(expenses));renderExpenses();showToast('Expense deleted')}));document.getElementById('exportExpenses').addEventListener('click',()=>{const blob=new Blob([JSON.stringify(expenses,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='velora-expenses.json';a.click();URL.revokeObjectURL(a.href)})}
